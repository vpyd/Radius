# area of a circle with radius input
radius = float(input("Input the radius of the circle : "))
import math
Area = float(math.pi* (radius*radius))
print("The area of the circle with radius 1.1 is :",Area)

